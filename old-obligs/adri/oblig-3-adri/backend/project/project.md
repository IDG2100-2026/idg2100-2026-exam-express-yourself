# PROJECT INFO

## Spanish Dice Poker Platform - Backend
REST API backend built with Node.js, Express.js, MongoDB and Mongoose, following MVCS architecture (without the V, + R).

## How to run
1. Install dependencies: `npm install`
2. Start server: `npm run dev`
3. Seed database: `npm run seed`
